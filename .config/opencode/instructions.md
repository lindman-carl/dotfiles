# Agent Instructions

## Markdown Files

Whenever you create or edit a `.md` file, run `markdownlint` on it immediately
after writing and fix every reported violation before declaring the task done.

```bash
pnpm dlx markdownlint-cli --fix <file>
pnpm dlx markdownlint-cli <file>
```
